services:
  - type: web
    name: trustretail-ai-inventory
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: VITE_GEMINI_API_KEY
        sync: false
